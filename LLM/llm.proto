syntax = "proto3";

package llm;

service LLMService {
  rpc Generate(GenerateRequest) returns (GenerateReply);
}

message GenerateRequest {
  string prompt = 1;
}

message GenerateReply {
  string response = 1;
}
